# Temporary port-analysis workspace

This folder is intentionally disposable. It contains the `olefile` Python environment and inspection scripts used to establish the source inventory recorded in `../PLAN.md`.

It is not a runtime dependency of the browser game. Do not publish the virtual environment or generated inspection output.
